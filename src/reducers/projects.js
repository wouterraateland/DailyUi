const initialState = [
  'Sign up for pizza',
  'Credit Card Checkout',
  'Above the fold',
  'Graph calculator',
]

const projectsReducer = (state=initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default projectsReducer
